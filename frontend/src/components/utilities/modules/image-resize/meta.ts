import { Scaling } from 'lucide-react';

import type { UtilityMeta } from '../../types';

const meta: UtilityMeta = {
  id: 'image-resize',
  name: 'Image Resize & Convert',
  description: 'Resize or convert images between PNG, JPEG and WebP, one file or a whole batch.',
  category: 'image',
  icon: Scaling,
  keywords: ['resize', 'scale', 'convert', 'png', 'jpg', 'jpeg', 'webp', 'avif', 'batch'],
};

export default meta;
