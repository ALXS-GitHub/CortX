import { KeyRound } from 'lucide-react';

import type { UtilityMeta } from '../../types';

const meta: UtilityMeta = {
  id: 'id-generator',
  name: 'ID & Secret Generator',
  description: 'UUID, ULID, nano IDs, passwords and API keys — with presets or your own format.',
  category: 'dev',
  icon: KeyRound,
  keywords: ['uuid', 'guid', 'ulid', 'nanoid', 'password', 'passphrase', 'secret', 'token', 'api key', 'random', 'hex', 'base64'],
};

export default meta;
