import { Shrink } from 'lucide-react';

import type { UtilityMeta } from '../../types';

const meta: UtilityMeta = {
  id: 'image-compress',
  name: 'Image Compression',
  description: 'Shrink images to a quality level or to a target file size, with before/after sizes.',
  category: 'image',
  icon: Shrink,
  keywords: ['compress', 'optimize', 'shrink', 'file size', 'quality', 'jpeg', 'webp', 'batch'],
};

export default meta;
