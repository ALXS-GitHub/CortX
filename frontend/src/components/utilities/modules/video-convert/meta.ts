import { Film } from 'lucide-react';

import type { UtilityMeta } from '../../types';

const meta: UtilityMeta = {
  id: 'video-convert',
  name: 'Video to GIF, Trim & Audio',
  description: 'Turn a clip into a GIF, cut a section out, or pull the audio track — via ffmpeg.',
  category: 'media',
  icon: Film,
  keywords: ['video', 'gif', 'trim', 'cut', 'audio', 'mp3', 'ffmpeg', 'convert', 'clip'],
};

export default meta;
